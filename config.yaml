log_level: debug # error, notice, info, debug

# agent:
#   api_key: "your-api-key"  # optional, uses kimi default if not set
#   model: "your-model"      # optional, uses kimi default if not set

watch:
  ignore_patterns:
    - ".git"
    - "node_modules"
    - ".memo"
    - "*.log"
  debounce_ms: 5000
  max_wait_ms: 300000
