log_level: info # error, notice, info, debug

agent:
  api_key: "your-api-key-here"
  model: "your-model"

watch:
  ignore_patterns:
    - ".git"
    - "node_modules"
    - ".memo"
    - "*.log"
  debounce_ms: 5000
  max_wait_ms: 300000
